// Stub — see FightBreakdownCard.tsx for context.
// Accepts any props since callers may pass varying shapes (legacy callsites).
export function FighterCard(_props: Record<string, unknown>) {
  return null;
}
